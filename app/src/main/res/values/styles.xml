<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoTitle">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoTitle.FullScreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- toolbar菜单样式 -->
    <style name="Toolbar.Theme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!--     <item name="actionMenuTextColor">@android:color/white</item>-->
        <item name="minHeight">?attr/actionBarSize</item>
        <!--     <item name="minHeight">@dimen/toolbar_height</item>-->
        <!--     <item name="android:paddingLeft">@dimen/toolbar_spacing_horizontal</item>-->
<!--        <item name="android:paddingRight">@dimen/toolbar_spacing_horizontal</item>-->
        <item name="android:elevation">@dimen/toolbar_elevation</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@color/textColorSecondary</item>
        <!--        <item name="android:gravity">center</item>-->
    </style>

    <style name="AppCompatTextView" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/text_size_content</item>
        <item name="android:padding">4dp</item>
        <item name="android:textColor">@color/textColorPrimary</item>
    </style>

    <style name="AppCompatButton" parent="Widget.AppCompat.Button">
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:textStyle">normal</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/text_size_sub_title</item>
    </style>

    <style name="AppCompatTextEdit" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/colorNormal</item>
        <item name="colorControlActivated">@color/colorPressed</item>
        <item name="android:textColor">@color/textColorPrimary</item>
    </style>

</resources>
